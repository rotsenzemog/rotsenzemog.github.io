name: Generar Borradores de Redes Sociales con IA

on:
  push:
    paths:
      - '_posts/**'
  workflow_dispatch: # Permite ejecución manual desde la pestaña Actions

jobs:
  generate-drafts:
    runs-on: ubuntu-latest
    permissions:
      contents: write # OBLIGATORIO: Permite a GitHub Actions guardar panel.html en tu repo
    steps:
      - name: Descargar repositorio
        uses: actions/checkout@v3
        with:
          fetch-depth: 0 # Necesario para revisar el historial de Git

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Instalar librerías
        run: |
          python -m pip install --upgrade pip
          pip install google-generativeai python-frontmatter requests

      - name: Ejecutar script de generación de copys
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          SITE_URL: "https://rotsenzemog.github.io/panel.html"
          FORCE_RUN: ${{ github.event_name == 'workflow_dispatch' }}
        run: python scripts/social_generator.py

      - name: Guardar cambios del Panel Web en el Repositorio
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add panel.html || true
          git commit -m "Update social panel HTML [skip ci]" || true
          git push
